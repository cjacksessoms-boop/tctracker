# For local development, leave this unset - the app defaults to
# http://localhost:3001 automatically.
#
# For a deployed/production build, set this to your live backend's URL
# (see the "Deploying" section in the root README.md), e.g.:
# VITE_API_BASE_URL=https://cyclone-tracker-backend.onrender.com
